name: pull-request

on:
  pull_request:
    branches: [ master ]

jobs:
  build:

    name: pull-request
    runs-on: ubuntu-latest

    # id-token: the coverage upload authenticates via GitHub OIDC (tokenless — the repo is
    # public, so coverage.mintplayer.com auto-provisions it; no COVERAGE_TOKEN secret needed).
    # packages: read — `dotnet restore` pulls from GPR with github.token.
    permissions:
      contents: read
      packages: read
      id-token: write

    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x
        # Authenticates packages to push to GPR
        source-url: https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
      env:
        NUGET_AUTH_TOKEN: '%NUGET_AUTH_TOKEN%'

    - name: Install dependencies
      run: dotnet restore
      env:
        NUGET_AUTH_TOKEN: ${{ github.token }}

    - name: Build
      run: dotnet build --configuration Release --no-restore -p:EnableSpaBuilder=false

    - name: Test
      # --no-build (already here since #43's timing pass) is what keeps coverage honest AND fast:
      # without it dotnet test silently rebuilds and measures that second build instead of the
      # Release binaries built above (Dotnet.Tools measured 88s -> 32s from this flag alone).
      #
      # --collect writes coverage.cobertura.xml into a GUID-named subdirectory of
      # --results-directory, hence the ** in the upload glob below.
      run: >
        dotnet test --configuration Release --no-build --filter "Category!=Slow&Category!=Determinism" --verbosity normal
        --settings coverlet.runsettings
        --collect:"XPlat Code Coverage" --results-directory coverage

    # M64.0c -- the bitwise-identity gate. Runs UNINSTRUMENTED and separately from the coverage
    # bucket: these compare checkpoint bytes against checked-in hash literals, so instrumentation
    # would cost time and change nothing. Until now this gate existed only as Slow-tagged tests that
    # CI never ran, which meant a change breaking training reproducibility shipped green.
    #
    # A failure here is NOT a flake. It means the trained bytes moved. Either that was intended (a
    # deliberate change to the training recipe -- update the literal and say so in the commit) or it
    # is exactly the regression this step exists to catch.
    - name: Determinism gate
      run: >
        dotnet test --configuration Release --no-build --filter "Category=Determinism"
        --verbosity normal

    # Nothing here gates the merge — the service publishes the `coverage/project` and
    # `coverage/patch` check runs itself; branch protection can require those.
    #
    # Placed before the Angular build: upload as soon as the report exists, so a broken
    # ClientApp doesn't cost the coverage datapoint.
    - name: Upload coverage
      # Fork PRs get neither secrets nor an OIDC token, so the upload cannot work there. Skip
      # rather than fail — a contributor's PR must not go red over that.
      # hashFiles guard: a run that produced no report is a no-op, not an upload of nothing.
      if: >
        github.event.pull_request.head.repo.full_name == github.repository
        && hashFiles('coverage/**/coverage.cobertura.xml') != ''
      continue-on-error: true
      uses: MintPlayer/MintPlayer.Spark/apps/CodeCoverage/action@coverage-upload-v1
      with:
        url: https://coverage.mintplayer.com
        use-oidc: true
        files: |
          coverage/**/coverage.cobertura.xml
        # With search on, a glob that matches nothing silently falls back to auto-detection
        # and would upload stray unparsable reports.
        disable-search: true
        base-sha: ${{ github.event.pull_request.base.sha }}
        # M65: the frontend uploads a SECOND report (hand-written TypeScript) under the same
        # (repo, sha, runId, runAttempt), and the service merges the two with max semantics. The
        # LAST upload finishes the build, so this one must not.
        finish: false
        # A coverage-service outage must never be the reason a PR cannot merge.
        fail-ci-if-error: false

    # The .NET steps skip the SPA (EnableSpaBuilder=false). Build the Angular app explicitly so a
    # broken ClientApp fails the PR — not the post-merge Docker build. Same command prod ships
    # (Dockerfile stage 1 `npm run build`), on the same Node major (node:22-alpine).
    - name: Setup Node
      uses: actions/setup-node@v5
      with:
        node-version: 22
        cache: npm
        cache-dependency-path: src/RLDemo.Web/ClientApp/package-lock.json

    - name: Build Angular app
      working-directory: src/RLDemo.Web/ClientApp
      run: npm ci && npm run build

    # The frontend suite, which DOES feed coverage again as of M65 -- but a different report from the
    # one M63.7 removed, and the difference is the whole point.
    #
    # M63.7 uploaded the Polyglot-GENERATED TypeScript twins, projected back onto the .pg sources.
    # That was a double-count: the C# side already covers those same .pg lines at 93.9% through the
    # #line pragmas, so it bought +0.1pp for +57s. It stays removed, and `coverageExclude` in
    # angular.json keeps `*_solver.ts` out of this report for exactly that reason.
    #
    # What uploads now is the HAND-WRITTEN ClientApp code, which the C# side cannot see at all. It is
    # scoped by `coverageInclude` to the modules that have specs rather than to all of src/**: the
    # renderers, Three.js hosts and Angular components are excluded deliberately (covering them means
    # faking a 2D canvas context to assert "it called fillRect", which measures nothing).
    #
    # tools/pg_coverage_remap.mjs is retained but NOT used here -- it exists to rewrite .ts
    # coordinates onto .pg line numbers, which is only needed for the gitignored twins. Hand-written
    # .ts is tracked in git, so the service resolves it directly.
    - name: Test Angular app
      working-directory: src/RLDemo.Web/ClientApp
      run: npm run test:coverage

    # The cobertura reporter writes project-relative paths with the platform separator. The coverage
    # service resolves a report path by suffix-matching it against `git ls-files`, which stores
    # forward slashes relative to the REPO root -- so `src\app\x.ts` matches nothing and the file is
    # silently dropped as unmatched. Normalise the separators and re-root the paths before uploading.
    - name: Re-root the frontend coverage paths
      if: always()
      working-directory: src/RLDemo.Web/ClientApp
      run: node ../../../tools/reroot_frontend_coverage.mjs coverage/ClientApp/cobertura-coverage.xml src/RLDemo.Web/ClientApp

    # The THIRD report, and the one that completes the .pg union.
    #
    # Each *_solver.ts is transpiled from a .pg that also produces the C# the backend runs, but the
    # two targets serve different callers: C# is the training agent, TypeScript is a visitor playing
    # in the browser. So a .pg line the browser reaches and training never does is genuinely
    # uncovered on the C# side -- measured, 137 such lines exist (the snake look-ahead planner and
    # the mountaincar policy net, neither of which had ANY test before M66).
    #
    # This projects the twins' coverage back onto .pg line numbers through the v3 source maps
    # Polyglot emits beside them. It has to be a separate report because the twins are gitignored
    # build outputs: the service resolves a path by suffix-matching `git ls-files`, so a report
    # naming a .ts twin is dropped without an error. The .pg IS tracked, so this one resolves.
    # (reroot_frontend_coverage.mjs strips the twins from the report above, so nothing double-counts.)
    - name: Project the Polyglot twins onto their .pg sources
      if: always()
      run: >
        node tools/pg_coverage_remap.mjs
        src/RLDemo.Web/ClientApp/coverage/ClientApp/coverage-final.json
        coverage-pg/pg.cobertura.xml .

    - name: Upload frontend coverage
      # Same guards as the C# upload: fork PRs have no OIDC, and a run that produced no report is a
      # no-op rather than an upload of nothing.
      if: >
        always()
        && github.event.pull_request.head.repo.full_name == github.repository
        && hashFiles('src/RLDemo.Web/ClientApp/coverage/ClientApp/cobertura-coverage.xml') != ''
      continue-on-error: true
      uses: MintPlayer/MintPlayer.Spark/apps/CodeCoverage/action@coverage-upload-v1
      with:
        url: https://coverage.mintplayer.com
        use-oidc: true
        files: |
          src/RLDemo.Web/ClientApp/coverage/ClientApp/cobertura-coverage.xml
        disable-search: true
        base-sha: ${{ github.event.pull_request.base.sha }}
        # The .pg report below is the last one, so it closes the build.
        finish: false
        fail-ci-if-error: false

    # Merged under the same (repo, sha, runId, runAttempt) as the C# report, which names the SAME
    # .pg files. The service's max semantics is what turns the two into a per-line union: a .pg line
    # counts as covered if either the training path or the browser path reached it.
    - name: Upload the .pg union coverage
      if: >
        always()
        && github.event.pull_request.head.repo.full_name == github.repository
        && hashFiles('coverage-pg/pg.cobertura.xml') != ''
      continue-on-error: true
      uses: MintPlayer/MintPlayer.Spark/apps/CodeCoverage/action@coverage-upload-v1
      with:
        url: https://coverage.mintplayer.com
        use-oidc: true
        files: |
          coverage-pg/pg.cobertura.xml
        disable-search: true
        base-sha: ${{ github.event.pull_request.base.sha }}
        # The last upload of the build closes it.
        finish: true
        fail-ci-if-error: false
